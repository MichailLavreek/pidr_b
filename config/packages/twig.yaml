twig:
    paths: ['%kernel.project_dir%/templates']
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    globals:
        defaultLocale: 'ua'
        locales:
          - 'ua'
          - 'ru'
          - 'en'

    form_themes:
        - 'easy_admin/form/fields.html.twig'
